# Cater Deploy Google Cloud Storage

Deploys the cater build output (default: &lt;project>/build/static) to a bucket (default: gs://&lt;your-google-cloud-project-name/>-&lt;your-project-package-name>).
